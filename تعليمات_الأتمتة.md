# إزاي تشغّل البناء الأوتوماتيكي للـ APK

أضفتلك ملف `.github/workflows/build-apk.yml` — ده اللي بيخلي GitHub يبني الـ APK لوحده.

## الخطوات (مرة واحدة بس)

### 1. ارفع المشروع على GitHub
لو المستودع مش موجود:
```bash
cd fath_kalam_android_project
git init
git add .
git commit -m "أول نسخة + بناء أوتوماتيكي"
git branch -M main
git remote add origin https://github.com/USERNAME/REPO_NAME.git
git push -u origin main
```
(غيّر USERNAME و REPO_NAME باسمك واسم المستودع)

### 2. لو المستودع موجود بالفعل
فقط انسخ مجلد `.github` داخل مشروعك على GitHub، واعمل push عادي:
```bash
git add .github
git commit -m "إضافة بناء APK أوتوماتيكي"
git push
```

## هيحصل إيه بعد كده؟

كل ما تعمل `git push` على فرع `main`:
1. GitHub هيشغّل خادم بناء تلقائي (Actions)
2. هيجهز بيئة Android + Java
3. هيبني ملف APK
4. هيرفعه في مكانين:
   - **Artifacts**: تحت تبويب Actions → اختر آخر تشغيل → تلاقي `fath-kalam-apk` تقدر تنزله (بيفضل متاح 90 يوم)
   - **Releases**: هيعمل إصدار جديد اسمه `build-N` وفيه الـ APK جاهز رابط تحميل مباشر تحطه في المتصفح على الموبايل وتنزله وتثبته

## لو حابب تشغّله يدوي من غير push
روح لتبويب **Actions** في المستودع → اختار **Build APK** من القايمة اليسار → دوس **Run workflow**.

## ملاحظات مهمة
- الـ APK ده **Debug** (للتجربة والاستخدام الشخصي)، شغال فورًا بدون توقيع خاص.
- لو حبيت APK Release موقّع (لتوزيع أوسع)، محتاج تضيف مفتاح توقيع (keystore) كـ GitHub Secret — قولي لو عايز أظبطلك الخطوة دي كمان.
- في موبايلك (Android) هتحتاج تفعّل "تثبيت من مصادر غير معروفة" أول مرة عشان تركّب الـ APK يدوي بعد التحميل.
